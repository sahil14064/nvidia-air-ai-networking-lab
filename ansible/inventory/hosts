[leafs]
leaf[01:04]

[spines]
spine[01:02]

[switches:children]
leafs
spines


[servers]
server[01:04]

[switches:vars]
ansible_user      = "{{ ansible_user }}"
ansible_password  = "{{ ansible_password }}"         
ansible_become    = yes
ansible_become_method = sudo
ansible_become_pass   = "{{ ansible_become_pass }}"   
; ansible_network_os    = cumulus
ansible_connection    = network_cli
timezone              = Etc/UTC

; Common variables for servers
[servers:vars]
ansible_user      = "{{ ansible_server_user }}"
ansible_password  = "{{ ansible_server_password }}"               
ansible_become    = yes
ansible_become_method = sudo
ansible_become_pass   = "{{ ansible_server_become }}"
timezone              = Etc/UTC
